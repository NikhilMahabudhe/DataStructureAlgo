#include <iostream>
#include <cmath>
using namespace std;
int main() {
  double num, r;
  cout<<"Enter the number:"<<endl;
  cin>>num;
  cout<<"the absolute number is:"<< abs(num);
  return 0;
}